<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:padding="8dp"
    android:layout_margin="8dp"
    android:background="@color/primaryTextColor">

    <TextView
        android:id="@+id/user_position"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginStart="@dimen/player_score_margin"
        android:layout_marginLeft="@dimen/player_score_margin"
        android:textColor="@color/secondaryTextColor"
        android:textSize="@dimen/player_score_text_size"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        tools:text="14" />

    <ImageView
        android:id="@+id/user_image"
        android:layout_width="48dp"
        android:layout_height="48dp"
        android:layout_marginStart="@dimen/player_score_margin"
        android:layout_marginLeft="@dimen/player_score_margin"
        android:scaleType="centerCrop"
        android:src="@drawable/user_48px"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toEndOf="@+id/user_position"
        app:layout_constraintTop_toTopOf="parent" />

    <TextView
        android:id="@+id/user_name"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:layout_marginStart="@dimen/player_score_margin"
        android:layout_marginLeft="@dimen/player_score_margin"
        android:layout_marginEnd="@dimen/player_score_margin"
        android:layout_marginRight="@dimen/player_score_margin"
        android:ellipsize="end"
        android:textColor="@color/secondaryTextColor"
        android:textSize="@dimen/player_score_text_size"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toStartOf="@+id/user_score"
        app:layout_constraintStart_toEndOf="@+id/user_image"
        app:layout_constraintTop_toTopOf="parent"
        tools:text="George Sponge" />

    <TextView
        android:id="@+id/user_score"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginEnd="@dimen/player_score_margin"
        android:layout_marginRight="@dimen/player_score_margin"
        android:textColor="@color/secondaryTextColor"
        android:textSize="@dimen/player_score_text_size"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        tools:text="20.000" />

</androidx.constraintlayout.widget.ConstraintLayout>